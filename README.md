# نظام تسجيل الطلاب (Student Course Registration System) 🎓

هذا المشروع عبارة عن نظام متكامل لإدارة تسجيل الطلاب في الدورات التدريبية، تم تطويره باستخدام لغة **C++** مع تصميم قاعدة بيانات منظمة.

## 💡 فكرة المشروع
يهدف النظام إلى تسهيل العمليات الإدارية الأساسية المتعلقة بالطلاب والدورات، ويسمح للمستخدم بالقيام بـ:
1. **إدارة الطلاب:** إضافة بيانات طلاب جدد إلى النظام.
2. **إدارة الدورات:** تعريف وتحديث الدورات التدريبية المتاحة.
3. **التسجيل:** ربط الطلاب بالدورات المناسبة لهم.
4. **التقارير:** عرض قوائم كاملة للطلاب، الدورات، وحركات التسجيل التي تمت.

---

## 📂 هيكلية المشروع (Project Structure)
تم تنظيم الملفات في مجلدات منفصلة لضمان سهولة التعديل والصيانة:

* **`src/`**: يحتوي على الكود المصدري للمشروع، مقسماً إلى ملفات Header (`.h`) وملفات تنفيذية (`.cpp`) لكل من (Student, Course, Registration)، بالإضافة لملف `main.cpp`.
* **`database/`**: يحتوي على ملفات **MySQL**، بما في ذلك سكربتات إنشاء الجداول (Tables) والبيانات الأولية.
* **`docs/`**: يتضمن التوثيق الكامل للمشروع (ملف Word) بالإضافة إلى مخطط **ERD** الذي يوضح علاقات قاعدة البيانات.



---

## 🛠️ التقنيات المستخدمة
* **Language:** C++
* **Database:** MySQL
* **Tools:** (MySQL Workbench)

---

## 🚀 كيف تبدأ؟
1. قم باستيراد ملف الـ SQL الموجود في مجلد `database` إلى قاعدة البيانات لديك.
2. تأكد من إعداد مكتبة الربط بين C++ و MySQL.
3. قم بعمل Compile للملفات الموجودة في مجلد `src`.
